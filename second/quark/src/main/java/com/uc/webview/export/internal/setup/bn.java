package com.uc.webview.export.internal.setup;

import com.uc.webview.export.extension.UCCore;

/* compiled from: ProGuard */
public final class bn extends be {
    public final void run() {
        ((s) ((s) ((s) ((s) ((s) ((s) setup(UCCore.OPTION_DEX_FILE_PATH, null)).setup(UCCore.OPTION_SO_FILE_PATH, null)).setup(UCCore.OPTION_RES_FILE_PATH, null)).setup(UCCore.OPTION_UCM_LIB_DIR, null)).setup(UCCore.OPTION_UCM_KRL_DIR, null)).setup(UCCore.OPTION_UCM_CFG_FILE, null)).setup("scanUcmPrefix", "com.UCMobile");
        super.run();
    }
}
